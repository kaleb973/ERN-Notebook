import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "BenchNode",
  description: "BenchNode is a cutting-edge lab notebook application designed to streamline your scientific research workflow. With its intuitive interface and powerful features, BenchNode allows researchers to efficiently document experiments, manage data, and collaborate seamlessly. Whether you're in the lab or working remotely, BenchNode provides a secure and organized platform for all your research needs.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
